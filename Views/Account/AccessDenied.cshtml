@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="auth-wrap">
    <div style="text-align:center; font-size:48px; color:var(--danger); margin-bottom:10px">⚠</div>
    <h1>Acceso denegado</h1>
    <p class="sub">No tenés permiso para ver esta página.</p>

    <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-block" style="margin-top:10px">Volver al inicio</a>
</div>
